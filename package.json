{
  "name": "obsidian-copy-link",
  "version": "1.0.0",
  "description": "Adds a copy button next to links so you can copy the URL with one click.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "package": "npm run build && rm -rf dist && mkdir -p dist && cp main.js manifest.json styles.css dist/"
  },
  "keywords": ["obsidian", "plugin", "copy", "link"],
  "license": "MIT",
  "devDependencies": {
    "@codemirror/state": "^6.4.1",
    "@codemirror/view": "^6.26.0",
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.4.0"
  }
}
